# Pinned dependency stack for Google Colab (Python 3.12, T4, CUDA 12.4).
# Tested together with editable installs of nerfstudio, nerf-xray, xray-renderer, fem-lattice-simulator.
#
# Install via: scripts/install_colab_deps.sh (called from notebooks/00_ntnu_workshop_colab.ipynb)
#
# Design constraints:
#   - nerfstudio 1.0.3: protobuf<=3.20.3; nuscenes-devkit needs numpy<2
#   - jax 0.6.2: numpy>=1.26 (does NOT require numpy 2.x)
#   - fem_lattice_simulator: same numpy band as nerfstudio
#   - torch 2.6 + cu124: matches neural_xray/colab/demo.ipynb

--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# Shared numerical core (single numpy for FEM + nerfstudio + jax)
numpy==1.26.4
protobuf==3.20.3

# FEM / JAX (jax 0.6.2 minimum numpy is 1.26, not 2.0)
jax==0.6.2
jaxlib==0.6.2
ml_dtypes>=0.5.0
opt_einsum
scipy>=1.12
meshio
matplotlib
pyyaml>=6.0.3

# tinycudann: install wheel in install_colab_deps.sh (platform-specific URL)
